apiVersion: v1
kind: Service
metadata:
  name: charging-demand
  namespace: fastapi
spec:
  type: ClusterIP
  selector:
    app: charging-demand
  ports:
    - port: 80
      targetPort: 8001
      protocol: TCP
