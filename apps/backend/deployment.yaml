apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: backend
  labels:
    app: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: qp4534/ev-energy-management-backend:2a7a3a2acd88a5b9b69b936d4d897b3c54f44b8a
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          lifecycle:
            preStop:
              exec:
                # give the ingress a moment to stop routing here before the process dies
                command: ["sh", "-c", "sleep 5"]
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xmx256m -Xss512k"
            # Spring maps FASTAPI_BASE_URL to the fastapi.base-url property.
            - name: FASTAPI_BASE_URL
              value: "http://ev-ai-inference-api.fastapi.svc.cluster.local"
            # value comes from the "db-secret" Secret (created by the backend repo's
            # deploy workflow), not stored here
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_PASSWORD
          resources:
            requests:
              cpu: 100m
              memory: 200Mi
            limits:
              memory: 384Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
