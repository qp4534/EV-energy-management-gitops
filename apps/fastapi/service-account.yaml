apiVersion: v1
kind: ServiceAccount
metadata:
  name: ev-scenario-replay
  namespace: fastapi
  # The IAM role is bound through the EKS Pod Identity association
  # (namespace=fastapi, serviceAccount=ev-scenario-replay), not an annotation.
